import { useEffect, useContext } from 'react';

const Navbar = () => {
    return (
        <nav className='navbar nav'>
            Employee Directory
        </nav>
    );
}

export default Navbar;
